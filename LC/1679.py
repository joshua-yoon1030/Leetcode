def maxOperations(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
    #loop through nums
    #put every num, k-num into the dictionary
    